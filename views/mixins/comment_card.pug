
mixin comment_card(comment, user)
    div(class="card border-secondary mb-3" style="max-width:75%;margin:auto")
        div(class="card-header")= comment.date
        div(class="card-body")
            h5(class="card-title")= comment.text
            a(href=comment.user_url class="card-text")= user


    